<UserControl x:Class="SushiSushi.SecondarySideBarItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Border BorderBrush="Black" BorderThickness="2" HorizontalAlignment="Left" Height="120" Margin="50,5" VerticalAlignment="Top" Width="180">
            <Grid x:Name="MainGrid" Margin="0,0,0,0" Background="{Binding ElementName=SushiSushi.SecondarySideBarItemControl, Path=MainBackgroundProperty}">
                <Grid>
                    <Image HorizontalAlignment="Left"  Margin="13,28,0,0" VerticalAlignment="Top" Width="65" Height="32" Source="{Binding Image}"/>
                    <Label HorizontalAlignment="Right" Margin="0,28,33,0" VerticalAlignment="Top" Width="35" Height="32" Content="{Binding countOfItem}"/>
                    <Label Content="{Binding Name}" HorizontalAlignment="Left" Margin="15,5,0,0" VerticalAlignment="Top" Width="148" FontWeight="Bold" Height="18"/>
                    <Label Content="{Binding SelectedOption}" HorizontalAlignment="Left" Margin="15,65,0,0" VerticalAlignment="Top" Width="146" FontSize="10" FontWeight="Bold"/>
                    <Button x:Name="OrderAgainButton" Content="Please Order Again!" Width="150" Height="28" Margin="13,0,13,5" VerticalAlignment="Bottom" FontWeight="Bold" Click="OrderAgainButton_Click"/>
                </Grid>
            </Grid>
        </Border>
        <!--
        PUT EVERYTHING CHANGED OF ORDERED/DELIVERED CONTROLS. 
        TOO MUCH IS DIFFRENT SO WE SHOULD AIM TO USE A DIFFERENT CONTROL.
        
        -->

    </Grid>
</UserControl>
