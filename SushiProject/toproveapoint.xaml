<UserControl x:Class="SushiSushi.toproveapoint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
           
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="Auto">
    <Grid>
        <ListBox x:Name="ItemsListBox" ScrollViewer.VerticalScrollBarVisibility="Disabled">
           <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Width="500" Height="Auto" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
           <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Width="100" Height="100">
                        <Button Width="100" Height="100" Content="{Binding name}" Background="Beige"/>
                    </Grid> 
                 </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
